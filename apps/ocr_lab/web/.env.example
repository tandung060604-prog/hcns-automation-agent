# Hidden by default on the mentor-facing localhost dashboard.
# Set to true only in a private observation session.
VITE_SHOW_HELDOUT=false

# Legacy OCR/IDP upload remains available only for private diagnostics.
VITE_SHOW_LEGACY_UPLOAD=false
